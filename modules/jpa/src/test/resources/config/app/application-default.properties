# ---------------------------------------------------------------------------
# Persistence and database
# ---------------------------------------------------------------------------
database.user.login = root
database.user.password = password
database.url = jdbc:mysql://localhost:3306/justtesingdb?autoReconnect=true
database.hibernate.dialect = org.hibernate.dialect.MySQLDialect
database.datasource = org.mariadb.jdbc.Driver
database.hibernate.hbm2ddl.auto = create
database.hibernate.show.sql = true


#database.url = jdbc:h2:./target/.sample;INIT=create schema if not exists public
#database.hibernate.dialect = org.hibernate.dialect.H2Dialect

# Activate spring profiles
#spring.profiles.active = integrationTest
